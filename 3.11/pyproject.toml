[tool.poetry]
name = "docker-python"
version = "0.1.0"
description = ""
authors = []
packages = [{include = "3"}]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
